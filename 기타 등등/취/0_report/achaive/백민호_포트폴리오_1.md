# Minho Baek, Software Engineer

### Contact
- phone : 010-5021-0857
- E-mail : hoeo159@gmail.com
- GitHub : https://github.com/hoeo159
### Education
- 성균관대학교, 소프트웨어학과 학사 (2020.03 ~ 2026.08)
	- GPA : 4.08 / 4.5
### About Me
&ensp;성균관대학교 소프트웨어학과에 재학 중이며 컴퓨터 그래픽스, GPU 프로그래밍, 게임 개발에 관심을 가지고 있습니다. CUDA 기반 병렬 알고리즘 개발과 OpenGL 기반 실시간 랜더링 프로젝트를 수행하였으며 최근에는 Unity와 LLM을 활용한 동적 인터랙티브 게임 시스템을 개발하고 있습니다.
### Technical Skills
- Programming
	- C/C++, Python, C#
- GPU / Parallel Computing
	- CUDA, OpenCL
- Graphics
	- OpenGL, GLSL
- Game Development
	- Unity
- AI / Data
	- PyTorch, SBERT, Vector Database

<div class="page-break" style="page-break-before: always;"></div>

# Experience

### Vueron Technology, Perception Engineer Intern (2023.04 ~ 2023.12)

#### 프로젝트 개요
&ensp;자율주행 객체 인지 모델의 성능을 분석하기 위해 모델 예측 결과를 시각적으로 확인하고 정량적으로 평가할 수 있는 도구를 개발했습니다. 또한 LiDAR 데이터 처리 과정의 병렬화를 통해 데이터 전처리 성능을 개선했습니다.
#### 주요 역할
##### 1. 딥러닝 모델 평가 툴 개발
- 객체 인지 모델의 prediction과 ground truth를 비교하여 IoU 기반 성능 평가 시스템 구현
- 매 프레임마다 검출 결과를 분석하여 정확도 및 이상치(outlier) 탐지 기능 구현
- OpenGL 기반 시각화 도구 개발을 통해 객체 인지 결과를 직관적으로 확인할 수 있는 환경 구축
##### 2. 데이터 전처리 병렬화
- 기존 serial 방식의 LiDAR data parsing pipeline을 병렬 처리 구조로 개선
- 타겟 보드 환경에 맞게 CUDA, OpenMP, OpenCL 기반 병렬 코드 구현
- Unit Test 및 Performance Test를 통해 GPU 메모리 사용량과 처리 성능 분석

#### 결과
- 40,000 ~ 80,000개의 LiDAR point 데이터를 병렬적으로 처리
- 데이터 전처리 단계에서 평균 1.2배 ~ 최대 2배 성능 향상(평균 22ms > 최대 10ms)
- 자율주행 perception 모델 성능을 정량적으로 분석할 수 있는 평가 지표 제공

<div style="text-align: center;">

![[뷰런_샘플_2.png|250]] &nbsp;&nbsp;&nbsp;&nbsp; ![[뷰런_샘플_4.png|350]]
<small>- 평가 툴 시각화(좌), 데이터 병렬화 pipeline 및 결과(우) -</small>

</div>
<div class="page-break" style="page-break-before: always;"></div>

# Main Projects

### 컴퓨터그래픽스 연구실 학부연구생 (2024.08 ~ 2025.07)

#### 프로젝트 개요

&ensp;성균관대학교 컴퓨터그래픽스 연구실에서 학부연구생으로 활동하며 두 가지 프로젝트를 담당했습니다. 첫째는 Glare Rendering을 Fourier Series 기반 모델로 개선하는 그래픽스 연구이며, 둘째는 IITP 과제로 진행한 CUDA 기반 Vector DB 인덱스 빌드 가속 연구입니다.

#### 주요 역할

##### 1. Fourier Series 기반 Real-time Glare Rendering

- 수십~수천 개의 ghost가 누적되어 형성되는 veiling glare에 대해, glare 개수 K에 선형 이상으로 비례하던 기존 방식의 한계를 극복하는 parametric 표현 연구([보고서 활동](https://github.com/hoeo159/report/tree/main/cg/vglare))
- 적분 형태의 후보 함수(다항식, 삼각함수, Fourier-Bessel, 2D Gaussian)를 비교, 1D series 곱으로 분해되는 2D Gaussian 채택
- **Precompute** : 삼각함수 덧셈정리로 ghost 좌표 ($x_k$, $y_k$)와 픽셀 ($x$, $y$)를 분리, K개 glare의 공통항 4종 coefficient를 `initial_update()`에서 누적해 SSBO에 적재
- **Render** : fragment shader는 N×M basis 가중합만 평가, mipmap min/max를 UBO로 셰이더에 전달해 동적 정규화로 saturation 해소
- 결과: 복잡도 O(K) → O(N²), K=1000 기준 0.311ms → 0.082ms, ghost 수와 무관하게 평균 4000–5000 FPS 유지(K : glare 개수, N : 푸리에 차수)

##### 2. CUDA 기반 Vector DB 인덱스 빌드 가속

- 1536차원의 고차원 대용량 데이터에 대한 GPU 파티셔닝 가속 담당, K-means kernel과 대용량 데이터 로딩 경로를 양 갈래로 최적화 ([기술 문서](https://cgskku.github.io/vdb/vdb/web/kmeans/))
- **K-means kernel (shared memory)** : centroid를 chunk 단위로 shared memory에 적재하고, 1 thread block ↔ 1 data point로 매핑해 각 thread가 sequential dimension을 처리하도록 재설계.
- **대용량 데이터 로딩 (pinned + async)** : GPU 메모리 한도를 초과하는 데이터셋을 위해 K-means 알고리즘과 분리된 데이터 전송 경로를 설계. host pinned memory와 GPU 메모리를 각각 두 영역으로 나눠 한쪽은 kernel 실행 ↔ 다른 쪽은 다음 batch 업로드를 병행하는 2-way async stream 파이프라인을 구성.
- 결과 : 1M × 1536, K=25, iter=10 기준 3.256s → 1.626s (NVIDIA 4090)


<div style="text-align: center;">

  

![[glare_diagram.png|400]]
  

<small>- FBO pipeline -</small>

  

</div>

<div style="text-align: center;">

  

![[vdb_dia2.png]]
  

<small>- GPU K-means -</small>

  

</div>

<div class="page-break" style="page-break-before: always;"></div>

### Generative AI 기반 SRPG (2025.09 ~ 2026.05)

#### 프로젝트 개요

&ensp;포스트 아포칼립스 세계관의 SRPG에서 플레이어의 행동과 대화 기록이 NPC의 태도, 이벤트 분기, 세계 상태에 누적으로 영향을 미치는 동적 인터랙티브 게임 시스템을 개발하였습니다. Unity 6.3 기반으로 거점(Hub) - 탐험(Expedition) 루프를 설계하고, LLM과 규칙 기반 AI를 결합하여 매 플레이마다 다른 서사가 만들어지는 구조를 구현했습니다. ([github](https://github.com/hoeo159/SKKU_SRPG_AI))

#### 주요 역할

##### 1. LLM 통합 및 프롬프트 엔지니어링

- OpenAI Responses API와 Unity `UnityWebRequest`를 통해 비동기 호출 클라이언트 구현
- 5종 세계 파라미터 + 8종 플레이어 성향 + NPC 호감도/메모리를 자연어 라벨로 변환하는 `PromptContextBuilder` 작성 (변수명·수치 노출 없이 정성적 표현으로 매핑)
- LLM 응답을 `reply / affinityDelta / memorySummary / tags` JSON 스키마로 파싱하고, 호감도 변화량 ±30 Clamp 및 응답 180자 제한으로 안정성 확보
- 사용자 입력을 별도 `PLAYER_SAYS` 블록으로 격리하여 프롬프트 인젝션 방어
- 탐험 종료 시 보고서 UI와 함께 다음 Hub 이벤트 LLM 생성을 백그라운드 Coroutine으로 처리하여, Hub 진입 시 캐싱된 결과를 즉시 표시

##### 2. Utility AI 기반 전투 시스템

- 12×12 격자 SRPG 환경에서 NPC가 다중 요소 점수 합으로 최적 행동을 선택하는 Utility AI 구현
- 공격력, 처치 가능성, 거리와 같은 5가지 요소와 동점 방지 노이즈, 위험 회피 등의 추가 로직을 통해 점수 계산
- BFS 경로 탐색으로 이동 가능 타일을 산출하고, 각 후보 위치마다 사정거리 내 모든 타겟을 평가

##### 3. 시스템 설계 및 모듈 통합

- `ScriptableObject` 기반 데이터 구조(`GameStateSO`, `EventCardSO`, `UnitDataSO`)로 게임 상태와 콘텐츠를 분리, 디자이너가 코드 수정 없이 이벤트·NPC를 추가할 수 있도록 설계
- 탐험 행동 통계(채집·대화·전투·회피)를 8종 성향 델타로 변환하는 `ProfileCalculator` 구현 
- 이벤트 선택은 성향 가중치 합산(`baseWeight + Σ(wTrait × normalizedTrait)`)에 이벤트 저장 큐를 결합하여 반복을 방지하면서도 플레이어 성향에 맞는 이벤트가 등장
- 닫힌 루프 설계 : 탐험 행동 → 성향 변화 → 이벤트 선택 가중치 → 이벤트 결과 → 세계 파라미터 변화로 이어지는 피드백 구조 구축
<div class="page-break" style="page-break-before: always;"></div>

#### 결과

- LLM 호출 결과를 이벤트 ID 단위로 캐싱하여 재진입 시 API 비용 절감 및 응답 일관성 확보
- 동일한 시나리오에서 플레이어 성향에 따라 서로 다른 이벤트, NPC 반응이 발생하는 동적 분기 시스템 완성
- 게임 기획, LLM 연동, 전투 AI, UI까지 개발자 관점에서 풀스택 게임 시스템을 설계, 구현 경험

<div style="text-align: center;">

  

![[탐험 진행(이동, 공격).png]]

  

<small>- 탐험 진행(이동, 공격) -</small>

  

</div>


<div style="text-align: center;">

  

![[대화와 이벤트.png|450]]

  

<small>- NPC 대화와 이벤트 생성 -</small>

  

</div>

<div class="page-break" style="page-break-before: always;"></div>

# Additional Projects

### 교내 공지사항 개선(KingoM Plus) (2024.09 ~ 2024.12)
#### 프로젝트 개요
&ensp;교내 공지사항이 여러 사이트로 분산되어 있고 기존 KingoM 검색을 자연어 질의를 처리하도록 고도화하기 위한 프로젝트. 공지사항을 통합하여 크롤링하고 한국어 SBERT 기반 의미 검색 파이프라인을 구축. 6인 팀 리더로 일정·역할을 조율하며 크롤러부터 벡터 DB 검색까지 백엔드 ML 파이프라인을 담당.

- 6개 학내 사이트 크롤러 통합 : 이종 HTML 구조(학과 5개 `<li>` + 기숙사 jsp `<table>`)를 공통 parser + dorm 전용 parser로 분리. `articleNo` 정규식 추출을 unique key로 크롤러→Sheets→Pinecone 적재
- KR-SBERT 도메인 특화 fine-tuning : 학과 별칭이나 줄임말 미학습 문제 해결을 위해, 부서/제목/본문 가중합 라벨링과 negative sampling으로 학습 셋 생성. AdamW + WarmupLinear로 10 epoch 학습 후 Pinecone serverless(cosine)에 적재.
- 임베딩 설계 : 본문 길이에 대해 제목 비중이 희석되는 문제 발견. 전처리에서 제목을 N번 반복해 추가 가중치 부여. 메타데이터에 원문 url-content를 동봉해 검색 결과 노출을 1-hop으로 단축.
- 검증 : 졸업 요건/장학금/기숙사 등 사용자 시나리오 기반 테스트 쿼리로 top-k를 비교, 정성 평가.


<div style="text-align: center;">

![[소공개_샘플_2.png|450]]
<small>- Vector Database를 이용한 공지사항 검색 -</small>

</div>


<div style="text-align: center;">

![[소공개_샘플_1.png|500]]
<small>- KingoM Plus 구현 모습 -</small>

</div>
